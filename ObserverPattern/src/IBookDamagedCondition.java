/**
 * Each class that wants to observe a change in the condition of the books has to implement the following interface by
 * adding logic into the implemented update method below.
 */
public interface IBookDamagedCondition {

    public void update();
}
